import React from "react";

import { Typography } from "@material-ui/core";

const FinalizeGroupCreation = props => {
  return (
    <div>
      <Typography variant="body1" gutterBottom>
        Thank you for completing the steps for creating a group. Click Submit to
        create the group or Back to make any changes.
      </Typography>
    </div>
  );
};

export default FinalizeGroupCreation;
